import numpy as np #import numpy as np

arr= np.array([1, 2, 3, 4, 5],ndmin=5) #create a 5D array using ndmin parameter to specify the number of dimensions

print(arr)#print the array
print(f'Number of dimensions: {arr.ndim}') #print the number of dimensions of the array